A simple web app for managing tables in a restaraunt using streamlit library.

streamlit version: 1.0.0

To install streamlit, run the following command in the terminal:
    "pip install streamlit"

Start the app by typing the following command in the terminal:
    'streamlit run main.py'